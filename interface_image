import tkinter as tk

scene_foret = tk.Tk()
scene_foret.geometry('693x591')
scene_foret.title('THE FOREST')
fond = tk.PhotoImage(file="theforest.png")
label1= tk.Label(scene_foret, image=fond)
label1.place(x=0, y=0)
scene_foret.mainloop()
